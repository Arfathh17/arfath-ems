Now save this `README.md` file inside your `lastmgt` folder, then run these in Git Bash:

```bash
git add .
git commit -m "added README"
git push
```

Then go to `github.com/Arfathh17/arfath-ems` and your README will show beautifully on the page! 🎉

After that we deploy to **Render** and you're fully done! 🚀😄